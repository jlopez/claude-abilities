---
description: Reconcile upstream ability changes with this repo's local evolution, conversationally; ends in a PR and an advanced baseline. Not implemented yet.
disable-model-invocation: true
argument-hint: "<ability-id>"
---

# /abilities:update — not implemented yet

Tell the user, plainly and briefly:

> `/abilities:update` is not implemented yet — it is **roadmap item 6**, the
> last of the core lifecycle commands, and depends on items 1, 2, 4, and 5.

What it will do (from the design, for context if the user asks): a
reconciliation conversation — which upstream changes to take, how they interact
with local evolution — ending in a PR, an advanced baseline version, and
appended prose notes in the adoption record. No silent three-way merges: the
agent proposes and explains, the human decides.

Do not attempt to improvise any of that behavior.
