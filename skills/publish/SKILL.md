---
description: Upstream a locally evolved ability — author a generalized ability from this repo's artifacts, as a PR to the base ability or a brand-new one. Not implemented yet.
disable-model-invocation: true
argument-hint: "[<ability-id>]"
---

# /abilities:publish — not implemented yet

Tell the user, plainly and briefly:

> `/abilities:publish` is not implemented yet — it is **roadmap item 7** and
> depends on the ability format (item 2) it would author into.

What it will do (from the design, for context if the user asks): the inverse
transpile — from this repo's evolved artifacts, author a generalized ability,
either as a PR against the base ability or as a brand-new ability, proposing
config points during generalization ("this script hardcodes pnpm — parameterize
the package manager?").

Do not attempt to improvise any of that behavior.
